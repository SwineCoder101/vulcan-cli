{
  "schema_version": "1.0.0",
  "cli_version": "0.6.4",
  "description": "Machine-readable command catalog for vulcan. 68 tools across 13 groups.",
  "groups": {
    "market": "Market data: prices, orderbooks, candles, funding rates",
    "trade": "Order management: place, cancel, TP/SL",
    "position": "Position management: view, close, reduce, bracket orders",
    "margin": "Collateral management: deposit, withdraw, transfer, leverage tiers",
    "history": "Phoenix/Rise trader history: fills, orders, collateral, funding, PnL",
    "status": "Health check, connectivity, and CLI update detection",
    "wallet": "Wallet creation, address, listing, and balance",
    "account": "Account info and registration",
    "auth": "Phoenix API wallet-session authentication",
    "portfolio": "Combined portfolio snapshot in a single fetch",
    "paper": "Local paper trading simulation with live prices and no real funds",
    "strategy": "Curated Vulcan-owned strategy runners with tick logs and reports",
    "ta": "Technical analysis: indicators (SMA, EMA, RSI, MACD, BBands, ATR, VWAP, ADX, Stoch) and trigger evaluation over candle history"
  },
  "commands": [
    {
      "name": "vulcan_market_list",
      "command": "vulcan market list",
      "group": "market",
      "description": "List all available perpetual markets on Phoenix DEX with fees and leverage info.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan market list -o json"
    },
    {
      "name": "vulcan_market_ticker",
      "command": "vulcan market ticker",
      "group": "market",
      "description": "Get real-time ticker data for a market: mark price, funding rate, 24h volume and change.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        }
      ],
      "example": "vulcan market ticker SOL -o json"
    },
    {
      "name": "vulcan_market_info",
      "command": "vulcan market info",
      "group": "market",
      "description": "Get detailed market configuration: tick size, fees, funding params, leverage tiers.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        }
      ],
      "example": "vulcan market info SOL -o json"
    },
    {
      "name": "vulcan_market_orderbook",
      "command": "vulcan market orderbook",
      "group": "market",
      "description": "Get L2 orderbook snapshot with bids, asks, mid price, and spread.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "depth",
          "type": "integer",
          "required": false,
          "default": 10,
          "description": "Number of price levels per side"
        }
      ],
      "example": "vulcan market orderbook SOL --depth 10 -o json"
    },
    {
      "name": "vulcan_market_candles",
      "command": "vulcan market candles",
      "group": "market",
      "description": "Get historical candlestick (OHLCV) data for a market.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "interval",
          "type": "string",
          "required": false,
          "default": "1h",
          "description": "Candle interval: 1m, 5m, 15m, 1h, 4h, 1d"
        },
        {
          "name": "limit",
          "type": "integer",
          "required": false,
          "default": 50,
          "description": "Max candles to return"
        }
      ],
      "example": "vulcan market candles SOL --interval 1h --limit 50 --with-indicators rsi,macd -o json"
    },
    {
      "name": "vulcan_trade",
      "command": "vulcan trade <market-buy|market-sell|limit-buy|limit-sell>",
      "group": "trade",
      "description": "Place a market or limit order. Specify size via EXACTLY ONE of `size` (BASE LOTS — the exchange's atomic units, NOT tokens; misreading this is the #1 cause of mis-sized orders), `tokens` (base-asset amount, e.g. 0.5 SOL), or `notional_usdc` (USDC value, e.g. 100). `tokens` and `notional_usdc` work for BOTH market and limit orders — prefer them whenever you are thinking in token or USDC terms. For lot conversion: 1 token = 10^base_lots_decimals lots (see vulcan_market_info).",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "side",
          "type": "string",
          "required": true,
          "description": "Order side"
        },
        {
          "name": "order_type",
          "type": "string",
          "required": true,
          "description": "Order type. Market executes immediately, limit rests on the book."
        },
        {
          "name": "size",
          "type": "number",
          "required": false,
          "description": "Size in BASE LOTS (exchange atomic units, NOT tokens). For a market with base_lots_decimals=2, 1 token = 100 lots. Prefer `tokens` or `notional_usdc` to avoid lot-vs-token confusion. Exactly one of size/tokens/notional_usdc is required."
        },
        {
          "name": "tokens",
          "type": "number",
          "required": false,
          "description": "Size in base-asset tokens (e.g. 0.5 SOL). Works for both market and limit orders."
        },
        {
          "name": "notional_usdc",
          "type": "number",
          "required": false,
          "description": "Size as USDC notional. For market orders, quoted at mid mark; for limit orders, divided by the limit `price` to derive tokens. Actual fill may differ from quote by spread + impact."
        },
        {
          "name": "price",
          "type": "number",
          "required": false,
          "description": "Limit price in USD (required for order_type=limit)"
        },
        {
          "name": "tp",
          "type": "number",
          "required": false,
          "description": "Optional take-profit price"
        },
        {
          "name": "sl",
          "type": "number",
          "required": false,
          "description": "Optional stop-loss price"
        },
        {
          "name": "isolated",
          "type": "boolean",
          "required": false,
          "description": "Use isolated margin (dedicated collateral per position)"
        },
        {
          "name": "collateral",
          "type": "number",
          "required": false,
          "description": "USDC collateral for isolated subaccount (requires isolated=true)"
        },
        {
          "name": "reduce_only",
          "type": "boolean",
          "required": false,
          "description": "Order can only reduce existing position"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan trade market-buy SOL --notional-usdc 100 --yes -o json"
    },
    {
      "name": "vulcan_trade_multi_limit",
      "command": "MCP only: vulcan_trade_multi_limit",
      "group": "trade",
      "description": "Place multiple post-only limit orders (bids and asks) in a single transaction. Optional per-leg `tp` and `sl` arm a take-profit / stop-loss bracket on that leg's resulting position size; when any leg carries tp/sl the submission switches to per-leg limit-order instructions bundled in one tx (slide is ignored on that path). Sizes are BASE LOTS (NOT tokens) — see vulcan_market_info for the lot-to-token conversion.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "bids",
          "type": "object[]",
          "required": true,
          "description": "Array of bid orders. `size` is BASE LOTS (NOT tokens). Optional per-leg tp/sl prices."
        },
        {
          "name": "asks",
          "type": "object[]",
          "required": true,
          "description": "Array of ask orders. `size` is BASE LOTS (NOT tokens). Optional per-leg tp/sl prices."
        },
        {
          "name": "slide",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Whether orders should slide to top of book if they would cross. Default false. Ignored when any leg has tp/sl."
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan_trade_multi_limit → { \"symbol\": \"SOL\", \"bids\": [{ \"price\": 140, \"size\": 25, \"tp\": 150, \"sl\": 135 }], \"asks\": [], \"acknowledged\": true }"
    },
    {
      "name": "vulcan_trade_orders",
      "command": "vulcan trade orders",
      "group": "trade",
      "description": "List open orders. Omit symbol to list across all markets.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": false,
          "description": "Market symbol, e.g. SOL. Omit to list all markets."
        }
      ],
      "example": "vulcan trade orders -o json"
    },
    {
      "name": "vulcan_trade_cancel",
      "command": "vulcan trade <cancel|cancel-all|cancel-tpsl>",
      "group": "trade",
      "description": "Cancel orders. `scope=ids` cancels specific orders (requires order_ids); `scope=all` cancels every open order for the market (requires symbol); `scope=all-markets` cancels every open order across every market (symbol ignored); `scope=tpsl` cancels take-profit/stop-loss bracket orders on the position (use tp/sl booleans).",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": false,
          "description": "Market symbol, e.g. SOL. Required for scope=ids, scope=all, and scope=tpsl. Ignored for scope=all-markets."
        },
        {
          "name": "scope",
          "type": "string",
          "required": true,
          "description": "What to cancel"
        },
        {
          "name": "order_ids",
          "type": "string[]",
          "required": false,
          "description": "Order IDs to cancel (required when scope=ids)"
        },
        {
          "name": "tp",
          "type": "boolean",
          "required": false,
          "description": "When scope=tpsl, cancel the take-profit (default false)"
        },
        {
          "name": "sl",
          "type": "boolean",
          "required": false,
          "description": "When scope=tpsl, cancel the stop-loss (default false)"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan trade cancel-all --yes -o json    # all markets; or `cancel-all SOL` for one market"
    },
    {
      "name": "vulcan_trade_set_tpsl",
      "command": "vulcan trade set-tpsl",
      "group": "trade",
      "description": "Set take-profit and/or stop-loss on an existing position. Auto-detects position side. Supports multiple levels per side and partial sizing via 'tp_levels' / 'sl_levels'. Use the legacy 'tp' / 'sl' fields for a single full-position exit. Sums of level sizes must not exceed the current position.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "tp",
          "type": "number",
          "required": false,
          "description": "Single take-profit price covering the full position. Mutually exclusive with tp_levels."
        },
        {
          "name": "sl",
          "type": "number",
          "required": false,
          "description": "Single stop-loss price covering the full position. Mutually exclusive with sl_levels."
        },
        {
          "name": "tp_levels",
          "type": "object[]",
          "required": false,
          "description": "Take-profit levels, e.g. [{price: 90, size: 0.5}, {price: 95, size: 0.5}]. Mutually exclusive with tp."
        },
        {
          "name": "sl_levels",
          "type": "object[]",
          "required": false,
          "description": "Stop-loss levels. Mutually exclusive with sl."
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan trade set-tpsl SOL --tp-level 160:0.5 --tp-level 170:0.5 --sl-level 140 --yes -o json"
    },
    {
      "name": "vulcan_position_list",
      "command": "vulcan position list",
      "group": "position",
      "description": "List all open positions across cross and isolated subaccounts. Each row carries side, size, entry, mark, unrealized_pnl, unrealized_pnl_pct (precomputed), initial_margin, maintenance_margin, liquidation_price, and subaccount_collateral/subaccount_index for isolated positions.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan position list -o json"
    },
    {
      "name": "vulcan_position_show",
      "command": "vulcan position show",
      "group": "position",
      "description": "Show detailed info for a specific position: PnL, margin, liquidation price, TP/SL.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        }
      ],
      "example": "vulcan position show SOL -o json"
    },
    {
      "name": "vulcan_position_close",
      "command": "vulcan position close",
      "group": "position",
      "description": "Close an entire position via market order on the opposite side.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan position close SOL --yes -o json"
    },
    {
      "name": "vulcan_position_close_all",
      "command": "vulcan position close-all",
      "group": "position",
      "description": "Close every open position across all markets and subaccounts via market orders on the opposite side. Used for emergency flatten flows.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan position close-all --yes -o json"
    },
    {
      "name": "vulcan_margin_status",
      "command": "vulcan margin status",
      "group": "margin",
      "description": "Show cross-margin (subaccount 0) status only: collateral, PnL, risk state, available to withdraw. For full account view including isolated subaccounts and total_account_value, call vulcan_portfolio.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan margin status -o json"
    },
    {
      "name": "vulcan_margin_deposit",
      "command": "vulcan margin deposit",
      "group": "margin",
      "description": "Deposit USDC collateral into the trading account.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "amount",
          "type": "number",
          "required": true,
          "description": "USDC amount to deposit"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin deposit 100 --yes -o json"
    },
    {
      "name": "vulcan_margin_withdraw",
      "command": "vulcan margin withdraw",
      "group": "margin",
      "description": "Withdraw USDC collateral from the trading account.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "amount",
          "type": "number",
          "required": true,
          "description": "USDC amount to withdraw"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin withdraw 50 --yes -o json"
    },
    {
      "name": "vulcan_margin_transfer",
      "command": "vulcan margin transfer",
      "group": "margin",
      "description": "Transfer collateral between subaccounts (e.g., cross-margin to isolated).",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "from_subaccount",
          "type": "integer",
          "required": true,
          "description": "Source subaccount index (0 = cross-margin)"
        },
        {
          "name": "to_subaccount",
          "type": "integer",
          "required": true,
          "description": "Destination subaccount index"
        },
        {
          "name": "amount",
          "type": "number",
          "required": true,
          "description": "USDC amount to transfer"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin transfer --from 0 --to 1 --amount 50 --yes -o json"
    },
    {
      "name": "vulcan_margin_transfer_child_to_parent",
      "command": "vulcan margin sweep",
      "group": "margin",
      "description": "Sweep all collateral from a child (isolated) subaccount back to cross-margin.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "child_subaccount",
          "type": "integer",
          "required": true,
          "description": "Child subaccount index to sweep"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin sweep --child 1 --yes -o json"
    },
    {
      "name": "vulcan_margin_sync_parent_to_child",
      "command": "vulcan margin sync",
      "group": "margin",
      "description": "Sync parent (cross-margin) state to a child (isolated) subaccount.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "child_subaccount",
          "type": "integer",
          "required": true,
          "description": "Child subaccount index to sync to"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin sync --child 1 --yes -o json"
    },
    {
      "name": "vulcan_margin_leverage_tiers",
      "command": "vulcan margin leverage-tiers",
      "group": "margin",
      "description": "Show leverage tier schedule for a market: max leverage and max size per tier.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        }
      ],
      "example": "vulcan margin leverage-tiers SOL -o json"
    },
    {
      "name": "vulcan_margin_add_collateral",
      "command": "vulcan margin add-collateral",
      "group": "margin",
      "description": "Add USDC collateral to an isolated position by symbol. Transfers from cross-margin to the isolated subaccount.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol of the isolated position, e.g. SOL"
        },
        {
          "name": "amount",
          "type": "number",
          "required": true,
          "description": "USDC amount to add"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan margin add-collateral SOL --amount 25 --yes -o json"
    },
    {
      "name": "vulcan_position_reduce",
      "command": "vulcan position reduce",
      "group": "position",
      "description": "Reduce a position by a specified size via market order on the opposite side.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "size",
          "type": "number",
          "required": true,
          "description": "Size to reduce by in base lots"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan position reduce SOL 25 --yes -o json"
    },
    {
      "name": "vulcan_position_tp_sl",
      "command": "vulcan position tp-sl",
      "group": "position",
      "description": "Attach take-profit and/or stop-loss bracket orders to an existing position.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "tp",
          "type": "number",
          "required": false,
          "description": "Take-profit price"
        },
        {
          "name": "sl",
          "type": "number",
          "required": false,
          "description": "Stop-loss price"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to confirm this dangerous operation"
        }
      ],
      "example": "vulcan position tp-sl SOL --tp 160 --sl 140 --yes -o json"
    },
    {
      "name": "vulcan_history",
      "command": "vulcan history <trades|orders|collateral|funding|pnl>",
      "group": "history",
      "description": "Get Phoenix/Rise trader history. `type` selects: trades (fills), orders, collateral, funding, or pnl.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [
        {
          "name": "type",
          "type": "string",
          "required": true,
          "description": "Which history feed to query"
        },
        {
          "name": "symbol",
          "type": "string",
          "required": false,
          "description": "Filter by market symbol (ignored for collateral and pnl)"
        },
        {
          "name": "resolution",
          "type": "string",
          "required": false,
          "default": "1h",
          "description": "PnL only: 1m, 5m, 15m, 1h, 4h, 1d, 1w, 1M"
        },
        {
          "name": "limit",
          "type": "integer",
          "required": false,
          "default": 20,
          "description": "Max results to return"
        },
        {
          "name": "cursor",
          "type": "string",
          "required": false,
          "description": "Pagination cursor for history feeds that support it"
        }
      ],
      "example": "vulcan history trades --symbol SOL --limit 100 -o json"
    },
    {
      "name": "vulcan_status",
      "command": "vulcan status",
      "group": "status",
      "description": "Health check: verify config, wallet, RPC, API, and trader registration status.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan status -o json"
    },
    {
      "name": "vulcan_update_check",
      "command": "vulcan update check",
      "group": "status",
      "description": "Check whether a newer Vulcan release is available on GitHub. Read-only: never modifies the binary. Returns current vs. latest version, a `update_available` boolean, the release URL, and an install-path-aware `update_command` hint (install.sh re-run for default/custom dirs, or 'use your package manager' for Homebrew/Nix/distro installs). Results are cached for 6 hours; pass `force_refresh: true` to bypass the cache. Surface the result to the user; do not run the update on their behalf.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "force_refresh",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Bypass the on-disk cache and hit the GitHub API directly."
        }
      ],
      "example": "vulcan update check -o json"
    },
    {
      "name": "vulcan_wallet_create",
      "command": "vulcan wallet create",
      "group": "wallet",
      "description": "Create a new encrypted local wallet and return its public deposit address. Requires a password used only for local encryption.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "Wallet name to create"
        },
        {
          "name": "password",
          "type": "string",
          "required": true,
          "description": "Password for encrypting the local wallet file"
        }
      ],
      "example": "vulcan wallet create --name main -o json"
    },
    {
      "name": "vulcan_wallet_list",
      "command": "vulcan wallet list",
      "group": "wallet",
      "description": "List all stored wallets with names, public keys, and default status. Includes fee_payer (the linked paymaster wallet name) when one is set.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan wallet list -o json"
    },
    {
      "name": "vulcan_wallet_address",
      "command": "vulcan wallet show",
      "group": "wallet",
      "description": "Return a wallet public deposit address without querying balances.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "description": "Wallet name (omit for default wallet)"
        }
      ],
      "example": "vulcan wallet show main -o json"
    },
    {
      "name": "vulcan_wallet_balance",
      "command": "vulcan wallet balance",
      "group": "wallet",
      "description": "Check SOL and USDC balance for a wallet.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "description": "Wallet name (omit for default wallet)"
        }
      ],
      "example": "vulcan wallet balance -o json"
    },
    {
      "name": "vulcan_wallet_set_fee_payer",
      "command": "vulcan wallet set-fee-payer",
      "group": "wallet",
      "description": "Link a stored wallet as the paymaster: it pays Solana transaction fees and registration rent for every subsequent transaction (register, deposit, withdraw, trades, closes, cancels) while the trader wallet still signs. The paymaster gains no authority over funds or positions; fund it with SOL. Takes effect on the next transaction in this session.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "Stored wallet name to use as paymaster"
        }
      ],
      "example": "vulcan wallet set-fee-payer sponsor -o json"
    },
    {
      "name": "vulcan_wallet_clear_fee_payer",
      "command": "vulcan wallet clear-fee-payer",
      "group": "wallet",
      "description": "Unlink the paymaster so the trader wallet pays its own transaction fees again.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan wallet clear-fee-payer -o json"
    },
    {
      "name": "vulcan_portfolio",
      "command": "vulcan portfolio",
      "group": "portfolio",
      "description": "Full account snapshot in one call: cross margin, positions across cross and isolated subaccounts (with unrealized_pnl_pct and initial_margin), resting orders + conditional TP/SL triggers, isolated_subaccounts[] per-iso summary, and a totals block (total_collateral / total_account_value / total_unrealized_pnl) across all subaccounts. Prefer totals.total_account_value over margin.portfolio_value when reporting account value.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [
        {
          "name": "include",
          "type": "string[]",
          "required": false,
          "description": "Optional subset of sections to return. Default: all three."
        }
      ],
      "example": "vulcan portfolio -o json"
    },
    {
      "name": "vulcan_paper_init",
      "command": "vulcan paper init",
      "group": "paper",
      "description": "Initialize or reset the local futures paper account. Uses live prices but no real funds.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "balance",
          "type": "number",
          "required": false,
          "default": 10000,
          "description": "Starting paper collateral balance"
        },
        {
          "name": "currency",
          "type": "string",
          "required": false,
          "default": "USDC",
          "description": "Currency label"
        },
        {
          "name": "fee_bps",
          "type": "number",
          "required": false,
          "default": 5,
          "description": "Fee in basis points charged on fills"
        }
      ],
      "example": "vulcan paper init --balance 10000 -o json"
    },
    {
      "name": "vulcan_paper_status",
      "command": "vulcan paper status",
      "group": "paper",
      "description": "Show paper account equity, PnL, fees, aggregate position notional, exposure ratio, positions, orders, and fill counts.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan paper status -o json"
    },
    {
      "name": "vulcan_paper_positions",
      "command": "vulcan paper positions",
      "group": "paper",
      "description": "List open paper positions with entry, mark, and unrealized PnL.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan paper positions -o json"
    },
    {
      "name": "vulcan_paper_orders",
      "command": "vulcan paper orders",
      "group": "paper",
      "description": "List open paper limit orders.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan paper orders -o json"
    },
    {
      "name": "vulcan_paper_fills",
      "command": "vulcan paper fills",
      "group": "paper",
      "description": "List recent paper fills.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "limit",
          "type": "integer",
          "required": false,
          "default": 50,
          "description": "Maximum fills to return"
        }
      ],
      "example": "vulcan paper fills --limit 50 -o json"
    },
    {
      "name": "vulcan_paper_trade",
      "command": "vulcan paper <buy|sell>",
      "group": "paper",
      "description": "Place a paper market or limit order. No real funds or Solana transactions are used. Optional 'tp' / 'sl' attach single-price TP/SL at order time: active immediately on a market fill, pending on a resting limit (re-parents on fill). Order-time TP/SL is rejected when the order would reduce an opposite-side existing position — place the order first, then use vulcan_paper_set_tpsl.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "side",
          "type": "string",
          "required": true
        },
        {
          "name": "order_type",
          "type": "string",
          "required": true,
          "default": "market"
        },
        {
          "name": "size",
          "type": "number",
          "required": false,
          "description": "Size in base lots"
        },
        {
          "name": "tokens",
          "type": "number",
          "required": false,
          "description": "Size in base-asset tokens"
        },
        {
          "name": "notional_usdc",
          "type": "number",
          "required": false,
          "description": "Size as USDC notional"
        },
        {
          "name": "price",
          "type": "number",
          "required": false,
          "description": "Limit price"
        },
        {
          "name": "tp",
          "type": "number",
          "required": false,
          "description": "Take-profit price attached at order time"
        },
        {
          "name": "sl",
          "type": "number",
          "required": false,
          "description": "Stop-loss price attached at order time"
        }
      ],
      "example": "vulcan paper buy SOL --tokens 1 --type market --tp 110 --sl 90 -o json"
    },
    {
      "name": "vulcan_paper_cancel",
      "command": "vulcan paper cancel",
      "group": "paper",
      "description": "Cancel a local paper order by ID.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "order_id",
          "type": "string",
          "required": true
        }
      ],
      "example": "vulcan paper cancel paper-order-1 -o json"
    },
    {
      "name": "vulcan_paper_cancel_all",
      "command": "vulcan paper cancel-all",
      "group": "paper",
      "description": "Cancel all local paper orders, optionally filtered by symbol.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": false
        }
      ],
      "example": "vulcan paper cancel-all SOL -o json"
    },
    {
      "name": "vulcan_paper_reconcile",
      "command": "vulcan paper reconcile",
      "group": "paper",
      "description": "Evaluate resting paper limit orders against live market prices and fill crossed orders.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": false
        }
      ],
      "example": "vulcan paper reconcile SOL -o json"
    },
    {
      "name": "vulcan_paper_set_tpsl",
      "command": "vulcan paper set-tpsl",
      "group": "paper",
      "description": "Set take-profit and/or stop-loss on an existing paper position. Mirrors `vulcan_trade_set_tpsl`: supports a single full-position price ('tp'/'sl') or multi-level laddered exits ('tp_levels'/'sl_levels') with explicit sizes. Sums of level sizes must not exceed the current paper position.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "tp",
          "type": "number",
          "required": false,
          "description": "Single take-profit price covering the full position. Mutually exclusive with tp_levels."
        },
        {
          "name": "sl",
          "type": "number",
          "required": false,
          "description": "Single stop-loss price covering the full position. Mutually exclusive with sl_levels."
        },
        {
          "name": "tp_levels",
          "type": "object[]",
          "required": false,
          "description": "Take-profit levels, e.g. [{price: 90, size: 0.5}, {price: 95, size: 0.5}]. Mutually exclusive with tp."
        },
        {
          "name": "sl_levels",
          "type": "object[]",
          "required": false,
          "description": "Stop-loss levels. Mutually exclusive with sl."
        }
      ],
      "example": "vulcan paper set-tpsl SOL --tp-level 160:0.5 --tp-level 170:0.5 --sl-level 140 -o json"
    },
    {
      "name": "vulcan_paper_cancel_tpsl",
      "command": "vulcan paper cancel-tpsl",
      "group": "paper",
      "description": "Cancel take-profit and/or stop-loss triggers on a paper position. Set the 'tp' / 'sl' booleans to choose which sides to clear.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "tp",
          "type": "boolean",
          "required": false,
          "description": "Cancel take-profit triggers"
        },
        {
          "name": "sl",
          "type": "boolean",
          "required": false,
          "description": "Cancel stop-loss triggers"
        }
      ],
      "example": "vulcan paper cancel-tpsl SOL --tp --sl -o json"
    },
    {
      "name": "vulcan_paper_triggers",
      "command": "vulcan paper triggers",
      "group": "paper",
      "description": "List active paper TP/SL triggers, optionally filtered by symbol.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": false,
          "description": "Optional market symbol filter"
        }
      ],
      "example": "vulcan paper triggers SOL -o json"
    },
    {
      "name": "vulcan_account_info",
      "command": "vulcan account info",
      "group": "account",
      "description": "Get trader account info: collateral, positions, risk state.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan account info -o json"
    },
    {
      "name": "vulcan_account_register",
      "command": "vulcan account register",
      "group": "account",
      "description": "Register and onboard a trader account via /v1/referral/activate-tx. Omit referral_code to register with the default code. Pass fee_payer (a stored wallet name) to have that wallet pay the fee and rent instead of the trader wallet.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "referral_code",
          "type": "string",
          "required": false,
          "description": "Referral code (optional; the default code is used when omitted)"
        },
        {
          "name": "fee_payer",
          "type": "string",
          "required": false,
          "description": "Stored wallet name that pays the transaction fee and trader-account rent (optional; sponsored registration, both wallets sign)"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to execute"
        }
      ],
      "example": "vulcan account register --yes -o json"
    },
    {
      "name": "vulcan_auth_status",
      "command": "vulcan auth status",
      "group": "auth",
      "description": "Show redacted Phoenix API wallet-session auth status. Does not expose tokens.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan auth status -o json"
    },
    {
      "name": "vulcan_auth_login",
      "command": "vulcan auth login",
      "group": "auth",
      "description": "Log in to the Phoenix API by signing a wallet nonce with the unlocked MCP session wallet. Requires --allow-dangerous and acknowledged=true.",
      "auth_required": true,
      "dangerous": true,
      "parameters": [
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to sign the auth challenge"
        }
      ],
      "example": "vulcan auth login --yes -o json"
    },
    {
      "name": "vulcan_auth_logout",
      "command": "vulcan auth logout",
      "group": "auth",
      "description": "Clear the stored Phoenix API auth session. Does not affect wallet files.",
      "auth_required": false,
      "dangerous": true,
      "parameters": [
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": true,
          "description": "Must be true to clear the auth session"
        }
      ],
      "example": "vulcan auth logout --yes -o json"
    },
    {
      "name": "vulcan_strategy_twap_start",
      "command": "vulcan strategy twap start",
      "group": "strategy",
      "description": "Run a TWAP strategy: market-order slices with lot-aware base-lot conversion, persisted slice ledger, and launch-time margin feasibility checks. Launch/monitoring contract: see CONTEXT.md § Strategy Monitoring (Detached Runs). TWAP narration: every tick is a fill — report planned vs executable slice, fill, cumulative VWAP, equity, and the full transaction signature per slice.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "side",
          "type": "string",
          "required": true
        },
        {
          "name": "notional_usdc",
          "type": "number",
          "required": false,
          "description": "Total strategy notional in USDC"
        },
        {
          "name": "tokens",
          "type": "number",
          "required": false,
          "description": "Total strategy size in base tokens"
        },
        {
          "name": "slices",
          "type": "integer",
          "required": true,
          "description": "Number of TWAP slices"
        },
        {
          "name": "interval_seconds",
          "type": "integer",
          "required": false,
          "default": 60,
          "description": "Seconds between slices"
        },
        {
          "name": "mode",
          "type": "string",
          "required": false,
          "default": "paper",
          "description": "paper (Paper mode), confirm_each (Live mode with confirmation required), auto_execute (Live mode with automatic execution), or dry_run (Plan mode with dry run)"
        },
        {
          "name": "margin_mode",
          "type": "string",
          "required": false,
          "default": "cross",
          "description": "Live margin mode. Ask the user whether they want cross-margin or isolated-margin execution before launch."
        },
        {
          "name": "isolated_collateral",
          "type": "number",
          "required": false,
          "description": "Optional USDC collateral to transfer from cross-margin on the first isolated live order"
        },
        {
          "name": "run_label",
          "type": "string",
          "required": false,
          "description": "Optional user-visible run label"
        },
        {
          "name": "max_total_notional_usdc",
          "type": "number",
          "required": false,
          "description": "Hard guardrail for total planned strategy notional"
        },
        {
          "name": "max_step_notional_usdc",
          "type": "number",
          "required": false,
          "description": "Hard guardrail for per-step planned notional"
        },
        {
          "name": "max_price_drift_bps",
          "type": "number",
          "required": false,
          "description": "Hard guardrail for mark-price drift from run start"
        },
        {
          "name": "max_exposure_ratio",
          "type": "number",
          "required": false,
          "description": "Hard guardrail for position notional divided by equity; map user max-leverage safety input to this value"
        },
        {
          "name": "reconcile_attempts",
          "type": "integer",
          "required": false,
          "description": "Number of history reconciliation attempts per live step"
        },
        {
          "name": "reconcile_delay_ms",
          "type": "integer",
          "required": false,
          "description": "Milliseconds between reconciliation attempts"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": false,
          "description": "Required for live confirm_each or auto_execute; paper and dry_run do not require it"
        },
        {
          "name": "detached",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Return immediately with run_id; the agent enters the monitoring loop automatically (see CONTEXT.md § Strategy Monitoring (Detached Runs))"
        },
        {
          "name": "no_sleep",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Skip sleeping between slices for smoke tests"
        }
      ],
      "example": "vulcan strategy twap start --symbol SOL --side buy --notional-usdc 1000 --slices 5 --interval-seconds 30 --mode paper -o json"
    },
    {
      "name": "vulcan_strategy_grid_start",
      "command": "vulcan strategy grid start",
      "group": "strategy",
      "description": "Run a grid trading strategy: persisted level ledger, optional per-level TP/SL intent, replacement maintenance ticks, optional run-until-stopped lifecycle, stale-run detection, and local runner locking. Cross-margin uses multi-limit transactions; isolated submits individual limit orders and can transfer optional collateral on the first order. Live maintenance treats missing resting levels as filled and submits flipped replacements. Launch/monitoring contract: see CONTEXT.md § Strategy Monitoring (Detached Runs). Grid narration: most ticks are no-fill maintenance — send a concise heartbeat per tick (do not go silent); report fills/replacements in compact tables with full tx signatures. Per-level TP/SL is intent only until a bracket action is recorded. The runner is a local process, not a daemon — if lifecycle.stale=true, stop claiming the grid is maintained and tell the user to resume/reconcile. Use vulcan_strategy_finalize for explicit cleanup.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "lower_price",
          "type": "number",
          "required": false,
          "description": "Lower grid boundary price. Required unless center_on_mark is true."
        },
        {
          "name": "upper_price",
          "type": "number",
          "required": false,
          "description": "Upper grid boundary price. Required unless center_on_mark is true."
        },
        {
          "name": "center_on_mark",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Compute lower/upper from the current mark at launch. Requires width_pct. Prefer this over absolute bounds when the approval round-trip might let the mark drift outside a fixed range."
        },
        {
          "name": "width_pct",
          "type": "number",
          "required": false,
          "description": "Half-width of the grid as a percentage of mark (e.g. 1.0 = +-1%). Required when center_on_mark is true."
        },
        {
          "name": "levels_per_side",
          "type": "integer",
          "required": true,
          "description": "Number of bid levels below mark and ask levels above mark"
        },
        {
          "name": "tokens_per_level",
          "type": "number",
          "required": false,
          "description": "Generated level size in base tokens; mutually exclusive with size_lots_per_level"
        },
        {
          "name": "size_lots_per_level",
          "type": "integer",
          "required": false,
          "description": "Generated level size in base lots; mutually exclusive with tokens_per_level"
        },
        {
          "name": "bid_levels",
          "type": "string[]",
          "required": false,
          "description": "Custom bid levels as PRICE:SIZE_LOTS[:TP][:SL]"
        },
        {
          "name": "ask_levels",
          "type": "string[]",
          "required": false,
          "description": "Custom ask levels as PRICE:SIZE_LOTS[:TP][:SL]"
        },
        {
          "name": "take_profit_spacing",
          "type": "number",
          "required": false,
          "description": "Distance from entry to TP for generated levels"
        },
        {
          "name": "stop_loss_spacing",
          "type": "number",
          "required": false,
          "description": "Distance from entry to SL for generated levels"
        },
        {
          "name": "interval_seconds",
          "type": "integer",
          "required": false,
          "default": 60,
          "description": "Seconds between maintenance ticks"
        },
        {
          "name": "ticks",
          "type": "integer",
          "required": false,
          "default": 60,
          "description": "Maximum ticks to run, including initial placement"
        },
        {
          "name": "run_until_stopped",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Keep running maintenance ticks until paused or stopped"
        },
        {
          "name": "stale_after_seconds",
          "type": "integer",
          "required": false,
          "description": "Seconds without a tick before status reports this run as stale; defaults to max(2 * interval_seconds, 180)"
        },
        {
          "name": "mode",
          "type": "string",
          "required": false,
          "default": "paper",
          "description": "paper, dry_run, confirm_each, or auto_execute"
        },
        {
          "name": "margin_mode",
          "type": "string",
          "required": false,
          "default": "cross",
          "description": "Live margin mode. Ask the user whether they want cross-margin or isolated-margin execution before launch."
        },
        {
          "name": "isolated_collateral",
          "type": "number",
          "required": false,
          "description": "Optional USDC collateral to transfer from cross-margin on the first isolated live order"
        },
        {
          "name": "run_label",
          "type": "string",
          "required": false,
          "description": "Optional user-visible run label"
        },
        {
          "name": "slide",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Whether live multi-limit orders may slide to top of book if crossing"
        },
        {
          "name": "max_total_notional_usdc",
          "type": "number",
          "required": false,
          "description": "Max total planned grid notional"
        },
        {
          "name": "max_step_notional_usdc",
          "type": "number",
          "required": false,
          "description": "Max per-level notional"
        },
        {
          "name": "max_price_drift_bps",
          "type": "number",
          "required": false,
          "description": "Optional center-relative mark-price drift guard; grid bounds are the default price guard when omitted"
        },
        {
          "name": "max_exposure_ratio",
          "type": "number",
          "required": false,
          "description": "Max position notional divided by equity"
        },
        {
          "name": "reconcile_attempts",
          "type": "integer",
          "required": false,
          "description": "Number of reconciliation attempts"
        },
        {
          "name": "reconcile_delay_ms",
          "type": "integer",
          "required": false,
          "description": "Milliseconds between reconciliation attempts"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": false,
          "description": "Required for live confirm_each or auto_execute; paper and dry_run do not require it"
        },
        {
          "name": "detached",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Return immediately with run_id; the agent enters the monitoring loop automatically (see CONTEXT.md § Strategy Monitoring (Detached Runs))"
        },
        {
          "name": "no_sleep",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Skip sleeping between ticks for smoke tests"
        }
      ],
      "example": "vulcan strategy grid start --symbol SOL --lower-price 140 --upper-price 160 --levels-per-side 5 --tokens-per-level 0.5 --mode paper -o json"
    },
    {
      "name": "vulcan_strategy_runs",
      "command": "vulcan strategy runs",
      "group": "strategy",
      "description": "List persisted strategy run summaries.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "limit",
          "type": "integer",
          "required": false,
          "default": 20
        }
      ],
      "example": "vulcan strategy runs -o json"
    },
    {
      "name": "vulcan_strategy_status",
      "command": "vulcan strategy status <RUN_ID>",
      "group": "strategy",
      "description": "Show compact latest status and ticks for a persisted strategy run. By default this omits the full ledger to keep agent context small; set include_ledger=true only when needed.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "since_tick",
          "type": "integer",
          "required": false,
          "description": "Return ticks newer than this tick index in new_ticks"
        },
        {
          "name": "include_ledger",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Include the full persisted ledger; defaults to false to reduce context"
        }
      ],
      "example": "vulcan strategy status twap-... -o json"
    },
    {
      "name": "vulcan_strategy_monitor",
      "command": "vulcan strategy monitor <RUN_ID>",
      "group": "strategy",
      "description": "Return compact, non-blocking monitor state for a strategy run from persisted logs and ledger state: last observed tick, heartbeat age, expected next tick, runner lock, stale status, and event counts.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "include_ledger",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Include the full persisted ledger; defaults to false to reduce context"
        }
      ],
      "example": "vulcan strategy monitor grid-... -o json"
    },
    {
      "name": "vulcan_strategy_reconcile_grid",
      "command": "vulcan strategy reconcile-grid <RUN_ID>",
      "group": "strategy",
      "description": "Read-only inspection of live grid orders against the persisted grid ledger. Reports expected resting levels and missing levels without mutating ledger state.",
      "auth_required": true,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        }
      ],
      "example": "vulcan strategy reconcile-grid grid-... -o json"
    },
    {
      "name": "vulcan_strategy_wait_next_tick",
      "command": "vulcan strategy wait-next-tick <RUN_ID>",
      "group": "strategy",
      "description": "Wait server-side until a strategy emits a tick newer than after_tick, reaches a terminal status, or times out. Use this between detached MCP strategy ticks instead of shell sleep or repeated full status polls.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "after_tick",
          "type": "integer",
          "required": false,
          "description": "Return once a tick newer than this index exists. Omit on the first poll after a detached start to receive every tick already emitted (including ticks that landed before the call). For subsequent polls, pass the last tick index you have already narrated."
        },
        {
          "name": "timeout_seconds",
          "type": "integer",
          "required": false,
          "default": 90,
          "description": "Maximum seconds to wait before returning current status with timed_out=true. Server caps this at 300; longer values are silently clamped so the heartbeat contract holds on slow-cadence runs."
        },
        {
          "name": "include_ledger",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Include the full persisted ledger; defaults to false to reduce context"
        }
      ],
      "example": "vulcan strategy wait-next-tick twap-... --after-tick 1 -o json"
    },
    {
      "name": "vulcan_strategy_report",
      "command": "vulcan strategy report <RUN_ID>",
      "group": "strategy",
      "description": "Read the final or latest persisted report for a strategy run.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        }
      ],
      "example": "vulcan strategy report twap-... -o json"
    },
    {
      "name": "vulcan_strategy_pause",
      "command": "vulcan strategy pause <RUN_ID>",
      "group": "strategy",
      "description": "Request a running strategy to pause at the next safe point. Paused runs can be resumed.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "reason",
          "type": "string",
          "required": false,
          "description": "Optional reason recorded in the ledger"
        }
      ],
      "example": "vulcan strategy pause twap-... --reason user_requested -o json"
    },
    {
      "name": "vulcan_strategy_stop",
      "command": "vulcan strategy stop <RUN_ID>",
      "group": "strategy",
      "description": "Request a running strategy to stop permanently at the next safe point.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "reason",
          "type": "string",
          "required": false,
          "description": "Optional reason recorded in the ledger"
        }
      ],
      "example": "vulcan strategy stop twap-... --reason user_requested -o json"
    },
    {
      "name": "vulcan_strategy_finalize",
      "command": "vulcan strategy finalize <RUN_ID>",
      "group": "strategy",
      "description": "Request strategy stop and optionally perform explicit cleanup: cancel open orders on the strategy symbol and/or close any open position. Cleanup flags are dangerous and require acknowledged=true plus --allow-dangerous.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "reason",
          "type": "string",
          "required": false,
          "description": "Optional reason recorded in the ledger"
        },
        {
          "name": "cancel_orders",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Cancel open orders on the strategy symbol"
        },
        {
          "name": "close_position",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Close any open position on the strategy symbol"
        },
        {
          "name": "wait",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Wait for the runner to observe the stop request before cleanup"
        },
        {
          "name": "timeout_seconds",
          "type": "integer",
          "required": false,
          "default": 90,
          "description": "Maximum seconds to wait for runner progress"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": false,
          "description": "Required when cancel_orders or close_position is true"
        }
      ],
      "example": "vulcan strategy finalize grid-... --cancel-orders --close-position --wait --yes -o json"
    },
    {
      "name": "vulcan_strategy_preflight",
      "command": "vulcan strategy preflight",
      "group": "strategy",
      "description": "Inspect live-readiness for the active wallet without launching anything. Reports wallet identity, password availability (env vs MCP session vs missing), trader registration, collateral, and every blocker with a concrete remedy command. Call this BEFORE attempting any live strategy start to avoid allocating an aborted run.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [],
      "example": "vulcan strategy preflight -o json"
    },
    {
      "name": "vulcan_strategy_ta_start",
      "command": "vulcan strategy ta start",
      "group": "strategy",
      "description": "Start a TA-driven strategy run. `config` describes rules of (condition, action): each tick the runner evaluates rules top-down and the first whose composable boolean condition fires (and is not cooldown-locked) dispatches its action (open/close/reduce). Conditions compose `compare`, `crosses`, `all`/`any`/`not` over indicators, price, and constants. Reuses TWAP/grid scaffolding (run id, ledger, control IPC, pause/stop, monitor/status/wait_next_tick/report). Launch/monitoring contract: see CONTEXT.md § Strategy Monitoring (Detached Runs). TA narration: report which rule fired (or 'no rule matched') per tick, plus the indicator values that drove the decision.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "config",
          "type": "object",
          "required": true,
          "description": "TA strategy config with symbol, interval_seconds, rules[]."
        },
        {
          "name": "mode",
          "type": "string",
          "required": false,
          "default": "paper"
        },
        {
          "name": "max_ticks",
          "type": "integer",
          "required": false,
          "default": 60
        },
        {
          "name": "run_until_stopped",
          "type": "boolean",
          "required": false,
          "default": false
        },
        {
          "name": "run_label",
          "type": "string",
          "required": false
        },
        {
          "name": "detached",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Return immediately with run_id; the agent enters the monitoring loop automatically (see CONTEXT.md § Strategy Monitoring (Detached Runs))"
        },
        {
          "name": "max_total_notional_usdc",
          "type": "number",
          "required": false
        },
        {
          "name": "max_step_notional_usdc",
          "type": "number",
          "required": false
        },
        {
          "name": "max_price_drift_bps",
          "type": "number",
          "required": false
        },
        {
          "name": "max_exposure_ratio",
          "type": "number",
          "required": false
        },
        {
          "name": "reconcile_attempts",
          "type": "integer",
          "required": false
        },
        {
          "name": "reconcile_delay_ms",
          "type": "integer",
          "required": false
        },
        {
          "name": "no_sleep",
          "type": "boolean",
          "required": false,
          "default": false
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": false,
          "description": "Required for live modes."
        }
      ],
      "example": "vulcan strategy ta start --config-file strategy.json --mode paper --max-ticks 60 -o json"
    },
    {
      "name": "vulcan_ta_compute",
      "command": "vulcan ta compute",
      "group": "ta",
      "description": "Compute a single technical indicator (sma, ema, rsi, macd, bbands, atr, vwap, adx, stoch) over the latest candles for a market. Returns the series plus a summary verdict.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "indicator",
          "type": "string",
          "required": true,
          "description": "Indicator: sma | ema | rsi | macd | bbands | atr | vwap | adx | stoch"
        },
        {
          "name": "timeframe",
          "type": "string",
          "required": false,
          "default": "1h",
          "description": "Candle interval: 1m, 5m, 15m, 1h, 4h, 1d"
        },
        {
          "name": "period",
          "type": "integer",
          "required": false,
          "description": "Lookback period; falls back to the indicator's default when omitted"
        },
        {
          "name": "limit",
          "type": "integer",
          "required": false,
          "default": 200,
          "description": "Number of candles to fetch"
        },
        {
          "name": "params",
          "type": "object",
          "required": false,
          "description": "Indicator-specific parameters (e.g. MACD fast/slow/signal, BBands dev_up/dev_down)"
        }
      ],
      "example": "vulcan ta compute SOL --indicator rsi --timeframe 1h --period 14 -o json"
    },
    {
      "name": "vulcan_ta_signal",
      "command": "vulcan ta signal",
      "group": "ta",
      "description": "Evaluate a technical-analysis trigger spec against the latest indicator value. Returns { fired, value, threshold, op } so agents can gate decisions on conditions like 'RSI < 30' or 'MACD histogram crosses_above 0'.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "spec",
          "type": "object",
          "required": true,
          "description": "Trigger spec"
        }
      ],
      "example": "vulcan ta signal SOL --spec '{\"indicator\":\"rsi\",\"timeframe\":\"1h\",\"op\":\"lt\",\"threshold\":30}' -o json"
    },
    {
      "name": "vulcan_ta_report",
      "command": "vulcan ta report",
      "group": "ta",
      "description": "Bundled multi-indicator snapshot (RSI, MACD, BBands, ATR, ADX by default). Each indicator returns `latest` (named values from the most recent bar), `signals` (derived state — rsi.state, bbands.position_in_band, adx.trend_strength, macd.recent_cross, etc.), and `summary`. Full per-bar history is omitted by default to stay agent-friendly; pass `points_limit` to opt in. Use `vulcan_ta_compute` for the full series of one indicator.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "symbol",
          "type": "string",
          "required": true,
          "description": "Market symbol, e.g. SOL"
        },
        {
          "name": "timeframe",
          "type": "string",
          "required": false,
          "default": "1h",
          "description": "Candle interval"
        },
        {
          "name": "indicators",
          "type": "string[]",
          "required": false,
          "description": "Optional subset of indicators to compute. Omit for the default bundle."
        },
        {
          "name": "points_limit",
          "type": "integer",
          "required": false,
          "description": "Number of recent points to include per indicator. Default omits the full series; signals and summary are computed from the full untrimmed series regardless."
        }
      ],
      "example": "vulcan ta report SOL --timeframe 1h -o json"
    },
    {
      "name": "vulcan_strategy_resume",
      "command": "vulcan strategy resume <RUN_ID>",
      "group": "strategy",
      "description": "Resume a paused or incomplete persisted strategy run. Live runs are dynamically treated as dangerous and require acknowledged=true plus --allow-dangerous.",
      "auth_required": false,
      "dangerous": false,
      "parameters": [
        {
          "name": "run_id",
          "type": "string",
          "required": true
        },
        {
          "name": "from_step",
          "type": "integer",
          "required": false,
          "description": "Optional step index to resume from"
        },
        {
          "name": "acknowledged",
          "type": "boolean",
          "required": false,
          "description": "Required when resuming a live strategy"
        },
        {
          "name": "no_sleep",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Skip sleeping between steps for recovery smoke tests"
        }
      ],
      "example": "vulcan strategy resume twap-... --yes -o json"
    }
  ]
}
